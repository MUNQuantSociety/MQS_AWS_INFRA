# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/terraform.tfvars.example
# Saved plan files embed root variable values (incl. secrets) in cleartext.
*.tfplan
*.plan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
plan
# Env
.env
*.env
!.env.example

# OS
.DS_Store
